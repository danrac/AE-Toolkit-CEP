# Toolbox 2 — 0.1.31 Alpha 1

- Make all Toolbox menu, module, subsection, and dialog headings use consistent title case.
- Extend Import Source AE Projects to discover source links from image files using structured and flat AE metadata fields, file URLs, embedded XMP packets, and common `.xmp` sidecars.
- Show Comp Cleanup results as individual modules with visible checkboxes. SAFE TO REMOVE entries start selected; KEEP and AMBIGUOUS entries remain unchecked and protected.

Validation: the complete automated suite, ExtendScript ES3 syntax checks, client JavaScript syntax checks, release structure check, and package staging pass.

Install the attached signed ZXP, restart After Effects, and confirm **0.1.31 Alpha** in the topper.
