# Toolbox 2 — 0.1.17 Alpha 1

Fixes the duplicate 0.1.16 version used by an earlier local package. The distinct extension version and refreshed client cache identifiers allow installation as an upgrade.

Create project presets initially displays New. Clicking it reveals the full editor, including saved preset selection, project folders, comp naming, Add to template, and Save project preset. The separate Project templates panel is removed.

Automated tests and release checks passed. Native After Effects render and solid-conform testing remains pending.
