# Toolbox 2 — 0.1.32 Alpha 1

- Remove the numeric Out X, Out Y, In X, and In Y fields from Key Graph so the curve is edited directly with the draggable tangent handles.
- Make the Key Graph responsive: its height scales with the panel width while preserving the graph's 2:1 drawing ratio.

Validation: the complete automated suite, ExtendScript ES3 syntax checks, client JavaScript syntax checks, release structure check, and package staging pass.

Install the attached signed ZXP, restart After Effects, and confirm **0.1.32 Alpha** in the topper.
