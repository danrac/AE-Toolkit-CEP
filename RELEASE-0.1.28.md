# Toolbox 2 — 0.1.28 Alpha 1

- Add **Move animation keys to parent null** to Tools → Animation and layers.
- Transfer keyed Position, Scale, Rotation, and Orientation channels to a new parent null for each selected animated layer.
- Preserve keyframe timing and easing while resetting the transferred child channels to neutral values.
- Add CEP-safe hover and focus tooltips for toolbar buttons, including dynamically created controls.
- Document the animation transfer workflow in the panel guide.

Validation: the complete automated suite, ExtendScript ES3 syntax checks, keyed transform-transfer regression tests, client JavaScript syntax checks, release structure check, and package staging pass.

Install the attached signed ZXP, restart After Effects, and confirm **0.1.28 Alpha** in the topper.
